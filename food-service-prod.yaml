server:
  port: 7002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ntspbms615db
    username: root
    password: aadvik27
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        enable_lazy_load_no_trans: true

info:
  app:
    name: "Prod-Env"
    description: "This is Food Service Production environment"